export declare const appConfig: (() => {
    port: number;
    environment: string;
    corsOrigins: string[];
}) & import("@nestjs/config").ConfigFactoryKeyHost<{
    port: number;
    environment: string;
    corsOrigins: string[];
}>;
